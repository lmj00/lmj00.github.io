---
title: "RFC 868 Time Protocol — 1900년 1월 1일 이후 초를 32비트로 돌려주는 시각 동기화 프로토콜"
layout: post
categories: ai-notes
type: study-note
date: 2026-09-07 07:47:00 +0900
tags: [cs, network, rfc]
generated_by: "openrouter:deepseek/deepseek-v4-flash-0731"
generated_at: 2026-09-07
sources:
  - https://www.rfc-editor.org/rfc/rfc868
---

> 🤖 이 글은 공식문서를 근거로 **AI가 자동 생성**한 학습 노트입니다.

> 한 줄 요약: RFC 868은 TCP 또는 UDP 포트 37을 통해 1900년 1월 1일 00:00 GMT 이후 경과한 초를 32비트 이진 값으로 돌려주는, 사이트 독립적인 시각 동기화 프로토콜이다.

### 개요
RFC 868은 ARPA 인터넷 커뮤니티를 위한 표준으로, Time Protocol의 구현 방식을 규정한다. ARPA 인터넷에서 Time Protocol을 구현하기로 선택한 호스트는 이 표준을 채택하여 구현할 것이 기대된다. 이 프로토콜은 사이트에 독립적이고 기계가 읽을 수 있는 날짜와 시간을 제공하며, 요청을 보낸 출처에 1900년 1월 1일 자정 이후의 초 수를 돌려준다.

모든 시스템이 날짜/시간 클록을 갖춘 것은 아니며, 어떤 시스템이든 사람이나 기계의 오류로 잘못된 시각을 가질 수 있다. Time Protocol은 네트워크상의 여러 독립된 사이트를 짧게 폴링하여 시스템이 가진 시각을 빠르게 확인하거나 교정할 수 있게 해 준다.

### 시간 값: 1900년 1월 1일부터의 초
Time Protocol이 돌려주는 시간은 1900년 1월 1일 00:00(자정) GMT 이후 경과한 초 수를 32비트 이진 숫자로 표현한 것이다. 값 1은 1900년 1월 1일 오전 12:00:01 GMT를 가리키며, 이 기준은 2036년까지 사용할 수 있다.

문서에 제시된 예시 값은 다음과 같다.

| 시간 값 | 대응 시각 (GMT) |
|---|---|
| 2,208,988,800 | 1970년 1월 1일 00:00 |
| 2,398,291,200 | 1976년 1월 1일 00:00 |
| 2,524,521,600 | 1980년 1월 1일 00:00 |
| 2,629,584,000 | 1983년 5월 1일 00:00 |
| -1,297,728,000 | 1858년 11월 17일 00:00 |

예시에 음수 값이 포함된 것은 1900년 이전의 시각도 이 방식으로 표현할 수 있음을 보여 준다.

### TCP에서의 동작
TCP 위에서 사용할 때 서버는 포트 37(8진수 45)에서 연결을 기다린다. 클라이언트가 포트 37에 연결하면 서버는 32비트 시간 값을 보내고 연결을 닫는다. 클라이언트는 시간 값을 받은 뒤 연결을 닫는다.

서버가 자신의 사이트에서 시간을 결정할 수 없다면, 연결을 거절하거나 아무것도 보내지 않고 연결을 닫아야 한다.

### UDP에서의 동작
UDP 위에서 사용할 때 서버는 포트 37에서 데이터그램을 기다린다. 클라이언트가 빈 데이터그램을 포트 37로 보내면, 서버는 그 데이터그램을 받고 32비트 시간 값을 담은 데이터그램을 응답으로 보낸다. 클라이언트는 시간 데이터그램을 받는다.

서버가 시간을 결정할 수 없다면 도착한 데이터그램을 폐기하고 아무 응답도 하지 않아야 한다.

### TCP와 UDP 동작 비교
두 전송 계층에서의 동작을 단계별로 비교하면 다음과 같다.

| 단계 | TCP | UDP |
|---|---|---|
| 대기 | 포트 37에서 연결 대기 | 포트 37에서 데이터그램 대기 |
| 요청 | 포트 37로 연결 | 빈 데이터그램 전송 |
| 응답 | 32비트 시간 값 전송 후 연결 종료 | 32비트 시간 값 데이터그램 전송 |
| 실패 처리 | 연결 거절 또는 아무것도 보내지 않고 종료 | 데이터그램 폐기, 무응답 |

### 정리
RFC 868 Time Protocol은 1900년 1월 1일 00:00 GMT 이후 경과한 초 수를 32비트 이진 값으로 표현하여 클라이언트에 돌려주는 단순한 프로토콜이다. TCP와 UDP 모두에서 동작하며, 두 경우 모두 포트 37을 사용한다. 서버가 시간을 알 수 없을 때의 실패 처리 방식도 전송 계층에 따라 명확히 정의되어 있다. 이 프로토콜의 핵심 가치는 여러 독립된 사이트를 짧게 폴링하여 시스템의 시각을 빠르게 확인하거나 교정할 수 있다는 점에 있다.

---
> 🤖 작성 모델: `deepseek/deepseek-v4-flash-0731` (OpenRouter)
> 
> 참고한 공식문서:
> - [https://www.rfc-editor.org/rfc/rfc868](https://www.rfc-editor.org/rfc/rfc868)
